import { PlaceholderView } from "@/components/ui/placeholder-view"
import { MapPin } from "lucide-react"

export function manageCustomerAddressView() {
  return <PlaceholderView title="จัดการที่อยู่" icon={MapPin} />
}

import { PlaceholderView } from "@/components/ui/placeholder-view"
import { FileImage } from "lucide-react"

export function slipGalleryView() {
  return <PlaceholderView title="จัดการภาพสลิป" icon={FileImage} />
}

import { PlaceholderView } from "@/components/ui/placeholder-view"
import { CreditCard } from "lucide-react"

export function beamPaymentsView() {
  return <PlaceholderView title="ประวัติการชำระเงิน" icon={CreditCard} />
}

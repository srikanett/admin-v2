import { PlaceholderView } from "@/components/ui/placeholder-view"
import { Receipt } from "lucide-react"

export function manageBillView() {
  return <PlaceholderView title="จัดการบิลรายจ่าย" icon={Receipt} />
}

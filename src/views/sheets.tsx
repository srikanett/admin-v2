import { PlaceholderView } from "@/components/ui/placeholder-view"
import { FolderOpen } from "lucide-react"

export function manageSheetView() {
  return <PlaceholderView title="จัดการชีต" icon={FolderOpen} />
}

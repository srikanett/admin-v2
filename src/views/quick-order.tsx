import { PlaceholderView } from "@/components/ui/placeholder-view"
import { Zap } from "lucide-react"

export function quickAddOrderView() {
  return <PlaceholderView title="สั่งซื้อด่วน" icon={Zap} />
}

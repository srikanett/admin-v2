import { PlaceholderView } from "@/components/ui/placeholder-view"
import { Settings } from "lucide-react"

export function manageLineBotView() {
  return <PlaceholderView title="ตั้งค่า LINE Bot" icon={Settings} />
}
